# Build artifacts — everything here is regenerated by the build scripts.
/build/
*.cpio.gz
*.cpio

# Kernel build tree and downloads
kernel/build/
kernel/src/
kernel/*.tar.*
kernel/linux-*/

# Userland sources and build trees
userland/build/
userland/src/
userland/busybox-*/
userland/bash-*/
userland/*.tar.*

# Compiled init
init/init
init/*.o

# Editor / OS noise
*.swp
*~
.DS_Store
